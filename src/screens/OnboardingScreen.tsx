/**
 * The first-launch onboarding flow was removed.
 * This compatibility module remains empty so stale external imports fail safely
 * during incremental builds; App.tsx no longer imports or renders it.
 */
export {};
